services:
  db:
    image: postgres:15-alpine
    container_name: lightmapper_db
    user: root
    environment:
      POSTGRES_DB: ${DB_NAME:-lightmapper}
      POSTGRES_USER: ${DB_USER:-lightmapper_user}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-lightmapper_pass}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-lightmapper_user}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - lightmapper_network
    restart: unless-stopped

  web:
    build: .
    container_name: lightmapper_web
    command: >
      sh -c "python manage.py collectstatic --noinput &&
      gunicorn --bind 0.0.0.0:8000 --workers 4 --access-logfile /app/logs/access.log --error-logfile /app/logs/error.log --log-level info config.wsgi:application"
    ports:
      - "8000:8000"
    env_file:
      - .env.example
    environment:
      - DB_HOST=db
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - static_volume:/app/static
      - images_volume:/app/media
      - logs_volume:/app/logs
    networks:
      - lightmapper_network
    restart: unless-stopped

volumes:
  postgres_data:
  static_volume:
  images_volume:
  logs_volume:

networks:
  lightmapper_network:
    driver: bridge
